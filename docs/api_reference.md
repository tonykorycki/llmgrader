# API Reference

## Planned Endpoints

GET /
Returns the main UI.

POST /chat (future)
Accepts a user message and returns an LLM-generated response.

POST /grade (future)
Accepts question text, student answer, reference solution, and grading notes.
Returns a JSON score and explanation.
